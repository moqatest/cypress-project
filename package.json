{
  "scripts": {
    "login": "cypress run --env grepTags=@login",
    "signup": "cypress run --env grepTags=@signup",
    "smoke": "cypress run --env grepTags=@smoke"
  },
  "dependencies": {
    "@cypress/grep": "^3.1.0",
    "cypress": "^11.2.0"
  },
  "devDependencies": {
    "cypress-mochawesome-reporter": "^3.2.3",
    "cypress-social-logins": "^1.14.0"
  }
}